# Система управления служебными автомобилями

RESTful API для управления доступностью служебных автомобилей в корпоративной системе. Позволяет сотрудникам получать список доступных автомобилей на основе их должности, категории комфорта и занятости транспортных средств.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Архитектура проекта](#архитектура-проекта)
- [Слои приложения](#слои-приложения)
- [Принципы SOLID](#принципы-solid)
- [Схема базы данных](#схема-базы-данных)
- [Установка и настройка](#установка-и-настройка)
- [API Документация](#api-документация)
- [Примеры использования](#примеры-использования)
- [Тестирование](#тестирование)

---

## 🛠 Технологический стек

- **PHP**: 8.2+
- **Laravel**: 12.0
- **Laravel Sanctum**: 4.0 (API аутентификация)
- **База данных**: SQLite (для разработки), PostgreSQL/MySQL (для production)

---

## 🏗 Архитектура проекта

Проект построен на принципах **Clean Architecture** и **Domain-Driven Design (DDD)** с четким разделением ответственности между слоями.

### Основные принципы архитектуры:

1. **Разделение ответственности** - каждый слой отвечает за свою область
2. **Инверсия зависимостей** - высокоуровневые модули не зависят от низкоуровневых
3. **Единственная ответственность** - каждый класс решает одну задачу
4. **Тестируемость** - все компоненты легко тестируются изолированно

### Поток данных:

```
HTTP Request 
  → Route (api.php)
    → Middleware (auth:sanctum)
      → Controller (CarController)
        → FormRequest (AvailableCarsRequest) [валидация]
          → Service (CarService) [бизнес-логика]
            → Repository (CarRepository) [работа с БД]
              → Model (Car, User, etc.)
                → Response (CarResource) [форматирование]
                  → JSON Response
```

---

## 📦 Слои приложения

### 1. **Presentation Layer (Слой представления)**

**Контроллеры** (`app/Http/Controllers/Api/`)
- `CarController` - обработка HTTP запросов
- Отвечает только за HTTP-специфичную логику
- Делегирует бизнес-логику в Service слой

**Form Requests** (`app/Http/Requests/`)
- `AvailableCarsRequest` - валидация входящих данных
- Преобразование запроса в нормализованный массив фильтров

**Resources** (`app/Http/Resources/`)
- `CarResource` - форматирование данных для API ответа
- Контроль структуры выходных данных

**Traits** (`app/Http/Traits/`)
- `ApiResponseHelper` - единый формат API ответов

### 2. **Application Layer (Слой приложения / Бизнес-логика)**

**Services** (`app/Services/`)
- `CarService` - оркестрация бизнес-логики
- Координирует работу репозиториев
- Содержит правила доступности автомобилей
- Не зависит от HTTP-слоя

**Интерфейсы** (`app/Repositories/*/`)
- `CarRepositoryInterface` - контракт для работы с данными автомобилей
- `UserRepositoryInterface` - контракт для работы с данными пользователей
- Позволяют легко заменять реализации и тестировать код

### 3. **Domain Layer (Слой домена)**

**Models** (`app/Models/`)
- `Car`, `CarModel`, `Driver`, `Trip`, `User`, `Position`, `ComfortCategory`
- Содержат бизнес-логику домена
- Определяют связи между сущностями
- Используют Eloquent ORM

### 4. **Infrastructure Layer (Слой инфраструктуры)**

**Repositories** (`app/Repositories/`)
- `CarRepository` (реализует `CarRepositoryInterface`) - работа с данными автомобилей
- `UserRepository` (реализует `UserRepositoryInterface`) - работа с данными пользователей
- Инкапсулируют SQL-запросы и логику выборки
- Позволяют легко менять источники данных через интерфейсы

**Миграции** (`database/migrations/`)
- Определяют структуру базы данных
- Версионирование схемы БД

---

## 🎯 Принципы SOLID

### **S - Single Responsibility Principle (Принцип единственной ответственности)**

✅ **Каждый класс имеет одну причину для изменения:**

- `CarController` - только обработка HTTP запросов
- `CarService` - только бизнес-логика доступности автомобилей
- `CarRepository` - только работа с данными автомобилей
- `AvailableCarsRequest` - только валидация и нормализация запроса
- `CarResource` - только форматирование ответа

### **O - Open/Closed Principle (Принцип открытости/закрытости)**

✅ **Классы открыты для расширения, закрыты для модификации:**

- `CarRepository` - можно добавить новые методы фильтрации без изменения существующих
- `CarService` - можно расширить логику доступности через новые приватные методы
- `CarResource` - можно добавить новые поля через приватные методы без изменения основной структуры

### **L - Liskov Substitution Principle (Принцип подстановки Лисков)**

✅ **Наследники должны заменять базовые классы без изменения поведения:**

- Все модели наследуются от `Model` и корректно используют его функциональность
- `CarResource` наследуется от `JsonResource` и корректно реализует `toArray()`

### **I - Interface Segregation Principle (Принцип разделения интерфейса)**

✅ **Клиенты не должны зависеть от методов, которые они не используют:**

- Репозитории разделены по доменам (`CarRepository`, `UserRepository`)
- Интерфейсы репозиториев содержат только необходимые методы для каждого домена
- `CarService` получает только нужные репозитории через DI
- Каждый репозиторий содержит только методы, специфичные для его домена

### **D - Dependency Inversion Principle (Принцип инверсии зависимостей)**

✅ **Зависимости должны быть направлены на абстракции, а не на конкретные реализации:**

- `CarService` зависит от интерфейсов репозиториев (`CarRepositoryInterface`, `UserRepositoryInterface`)
- `CarController` зависит от `CarService` (через конструктор)
- Репозитории реализуют интерфейсы, что позволяет легко заменять реализации
- Использование Dependency Injection через конструкторы
- Биндинги интерфейсов зарегистрированы в `AppServiceProvider`

**Пример инверсии зависимостей:**

```php
// CarController зависит от абстракции CarService
public function __construct(protected readonly CarService $carService) {}

// CarService зависит от интерфейсов репозиториев (не от конкретных классов)
public function __construct(
    private readonly CarRepositoryInterface $carRepository,
    private readonly UserRepositoryInterface $userRepository
) {}

// Репозитории реализуют интерфейсы
class CarRepository implements CarRepositoryInterface {
    public function baseQuery(): Builder {
        return Car::query()->with([...]);
    }
}

// Биндинги в AppServiceProvider
$this->app->bind(CarRepositoryInterface::class, CarRepository::class);
```

---

## 🗄 Схема базы данных

### ER-диаграмма связей:

```
┌─────────────────┐
│   users         │
├─────────────────┤
│ id (PK)         │
│ name            │
│ email           │
│ password        │
│ position_id (FK)│──┐
└─────────────────┘  │
                     │
┌─────────────────┐  │     ┌──────────────────────────┐
│  positions      │  │     │ position_comfort_category│
├─────────────────┤  │     ├──────────────────────────┤
│ id (PK)         │◄─┘     │ position_id (FK)         │
│ name            │        │ comfort_category_id (FK) │
└─────────────────┘        └──────────────────────────┘
        │                            │
        │                            │
        │                            ▼
        │                  ┌─────────────────┐
        │                  │comfort_categories│
        │                  ├─────────────────┤
        │                  │ id (PK)         │
        │                  │ name            │
        │                  │ level           │
        │                  └─────────────────┘
        │                            │
        │                            │
        ▼                            │
┌─────────────────┐                  │
│     trips       │                  │
├─────────────────┤                  │
│ id (PK)         │                  │
│ car_id (FK)     │──┐               │
│ user_id (FK)    │──┤               │
│ start_at        │  │               │
│ end_at          │  │               │
│ status          │  │               │
└─────────────────┘  │               │
                     │               │
                     │               │
        ┌────────────┘               │
        │                            │
        ▼                            │
┌─────────────────┐                  │
│     cars        │                  │
├─────────────────┤                  │
│ id (PK)         │                  │
│ car_model_id(FK)│──┐               │
│ driver_id (FK)  │──┤               │
│ license_plate   │  │               │
│ year            │  │               │
│ color           │  │               │
│ status          │  │               │
└─────────────────┘  │               │
                     │               │
        ┌────────────┘               │
        │                            │
        ▼                            │
┌─────────────────┐                  │
│   car_models    │                  │
├─────────────────┤                  │
│ id (PK)         │                  │
│ brand           │                  │
│ name            │                  │
│ comfort_category_id (FK)───────────┘
└─────────────────┘
        │
        │
        ▼
┌─────────────────┐
│    drivers      │
├─────────────────┤
│ id (PK)         │
│ name            │
│ phone           │
│ license_number  │
└─────────────────┘
```

### Описание таблиц:

#### `comfort_categories`
Категории комфорта автомобилей (Первая, Вторая, Третья).

#### `positions`
Должности сотрудников (Директор, Менеджер, Сотрудник).

#### `position_comfort_category` (pivot)
Связь многие-ко-многим между должностями и категориями комфорта.

#### `car_models`
Модели автомобилей (бренд + название) с привязкой к категории комфорта.

#### `drivers`
Водители, закрепленные за автомобилями.

#### `cars`
Конкретные автомобили с привязкой к модели, водителю и статусом.

#### `trips`
Поездки/бронирования автомобилей с временными интервалами.

#### `users`
Пользователи системы с привязкой к должности.

---

## 🚀 Установка и настройка

### 1. Клонирование и установка зависимостей

```bash
composer install
npm install
```

### 2. Настройка окружения

```bash
cp .env.example .env
php artisan key:generate
```

### 3. Настройка базы данных

В файле `.env`:
```env
DB_CONNECTION=sqlite
# или для production:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=car_management
# DB_USERNAME=root
# DB_PASSWORD=
```

Для SQLite:
```bash
touch database/database.sqlite
```

### 4. Миграции и сидеры

```bash
php artisan migrate --seed
```

### 5. Запуск сервера

```bash
php artisan serve
```

---

## 📚 API Документация

### Аутентификация

Все API endpoints требуют аутентификации через Laravel Sanctum. Получите токен:

```bash
POST /api/login
Content-Type: application/json

{
  "email": "director@example.com",
  "password": "password"
}
```

Ответ:
```json
{
  "token": "1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
}
```

Используйте токен в заголовке:
```
Authorization: Bearer {token}
```

### Endpoints

#### `GET /api/cars/available`

Получить список доступных автомобилей для текущего пользователя.

**Query параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `start_at` | datetime | Да | Дата и время начала поездки |
| `end_at` | datetime | Да | Дата и время окончания поездки (должна быть после start_at) |
| `car_model_id` | integer | Нет | Фильтр по ID модели автомобиля |
| `comfort_category_id` | integer | Нет | Фильтр по ID категории комфорта |

**Валидация:**
- `start_at` - required|date
- `end_at` - required|date|after:start_at
- `car_model_id` - nullable|integer|exists:car_models,id
- `comfort_category_id` - nullable|integer|exists:comfort_categories,id

**Логика доступности:**

Автомобиль доступен, если:
1. ✅ Категория комфорта автомобиля доступна для должности пользователя
2. ✅ Автомобиль не занят в запрашиваемый интервал времени
3. ✅ Автомобиль в статусе `available` (не в `maintenance`)

---

## 💡 Примеры использования

### Пример 1: Получить все доступные автомобили

**Запрос:**
```bash
curl -X GET "http://localhost:8000/api/cars/available?start_at=2025-11-15%2010:00:00&end_at=2025-11-15%2018:00:00" \
  -H "Authorization: Bearer 1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Accept: application/json"
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "license_plate": "A001AA",
      "year": 2023,
      "color": "Black",
      "model": {
        "id": 1,
        "brand": "Mercedes-Benz",
        "name": "S-Class",
        "comfort_category": {
          "id": 1,
          "name": "Первая"
        }
      },
      "driver": {
        "id": 1,
        "name": "Иван Иванов",
        "phone": "+79990000001"
      }
    },
    {
      "id": 2,
      "license_plate": "A002AA",
      "year": 2022,
      "color": "White",
      "model": {
        "id": 2,
        "brand": "BMW",
        "name": "7 Series",
        "comfort_category": {
          "id": 1,
          "name": "Первая"
        }
      },
      "driver": {
        "id": 2,
        "name": "Петр Петров",
        "phone": "+79990000002"
      }
    }
  ],
  "message": "Список доступных автомобилей успешно получен"
}
```

### Пример 2: Фильтрация по модели автомобиля

**Запрос:**
```bash
curl -X GET "http://localhost:8000/api/cars/available?start_at=2025-11-15%2010:00:00&end_at=2025-11-15%2018:00:00&car_model_id=1" \
  -H "Authorization: Bearer 1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Accept: application/json"
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "license_plate": "A001AA",
      "year": 2023,
      "color": "Black",
      "model": {
        "id": 1,
        "brand": "Mercedes-Benz",
        "name": "S-Class",
        "comfort_category": {
          "id": 1,
          "name": "Первая"
        }
      },
      "driver": {
        "id": 1,
        "name": "Иван Иванов",
        "phone": "+79990000001"
      }
    }
  ],
  "message": "Список доступных автомобилей успешно получен"
}
```

### Пример 3: Фильтрация по категории комфорта

**Запрос:**
```bash
curl -X GET "http://localhost:8000/api/cars/available?start_at=2025-11-15%2010:00:00&end_at=2025-11-15%2018:00:00&comfort_category_id=1" \
  -H "Authorization: Bearer 1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Accept: application/json"
```

### Пример 4: Комбинированная фильтрация

**Запрос:**
```bash
curl -X GET "http://localhost:8000/api/cars/available?start_at=2025-11-15%2010:00:00&end_at=2025-11-15%2018:00:00&car_model_id=1&comfort_category_id=1" \
  -H "Authorization: Bearer 1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Accept: application/json"
```

### Пример 5: Ошибка валидации

**Запрос (неверные данные):**
```bash
curl -X GET "http://localhost:8000/api/cars/available?start_at=2025-11-15%2018:00:00&end_at=2025-11-15%2010:00:00" \
  -H "Authorization: Bearer 1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Accept: application/json"
```

**Ответ:**
```json
{
  "message": "The end at field must be a date after start at.",
  "errors": {
    "end_at": [
      "The end at field must be a date after start at."
    ]
  }
}
```

---

## 📊 Структура данных

### Модель Car (автомобиль)

```json
{
  "id": 1,
  "license_plate": "A001AA",
  "year": 2023,
  "color": "Black",
  "model": {
    "id": 1,
    "brand": "Mercedes-Benz",
    "name": "S-Class",
    "comfort_category": {
      "id": 1,
      "name": "Первая"
    }
  },
  "driver": {
    "id": 1,
    "name": "Иван Иванов",
    "phone": "+79990000001"
  }
}
```

### Модель User (пользователь)

```php
{
  "id": 1,
  "name": "Иван Директоров",
  "email": "director@example.com",
  "position": {
    "id": 1,
    "name": "Директор",
    "comfort_categories": [
      {
        "id": 1,
        "name": "Первая",
        "level": 1
      },
      {
        "id": 2,
        "name": "Вторая",
        "level": 2
      },
      {
        "id": 3,
        "name": "Третья",
        "level": 3
      }
    ]
  }
}
```

### Модель Trip (поездка)

```php
{
  "id": 1,
  "car_id": 1,
  "user_id": 1,
  "start_at": "2025-11-15 10:00:00",
  "end_at": "2025-11-15 18:00:00",
  "status": "planned" // planned, in_progress, completed, cancelled
}
```

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
php artisan test

# Только Feature тесты
php artisan test --testsuite=Feature

# Конкретный тест
php artisan test --filter CarAvailableTest

# С покрытием кода
php artisan test --coverage
```

### Покрытие тестами

Проект включает Feature тесты для API endpoint:

- ✅ **test_filter_by_car_model** - фильтрация по модели автомобиля
- ✅ **test_exclude_busy_cars** - исключение занятых автомобилей
- ✅ **test_only_available_comfort_categories_for_position** - доступность только категорий должности
- ✅ **test_user_without_position_sees_no_cars** - пользователь без должности
- ✅ **test_cars_in_maintenance_status_are_excluded** - исключение автомобилей в maintenance

---

## 📁 Структура проекта

```
app/
├── Http/
│   ├── Controllers/
│   │   └── Api/
│   │       └── CarController.php          # HTTP контроллер
│   ├── Requests/
│   │   └── Car/
│   │       └── AvailableCarsRequest.php   # Валидация запроса
│   ├── Resources/
│   │   └── CarResource.php                # Форматирование ответа
│   └── Traits/
│       └── ApiResponseHelper.php          # Единый формат ответов
├── Models/
│   ├── Car.php                            # Модель автомобиля
│   ├── CarModel.php                       # Модель автомобильной модели
│   ├── ComfortCategory.php                # Категория комфорта
│   ├── Driver.php                         # Водитель
│   ├── Position.php                       # Должность
│   ├── Trip.php                           # Поездка
│   └── User.php                           # Пользователь
├── Repositories/
│   ├── Car/
│   │   ├── CarRepositoryInterface.php     # Интерфейс репозитория автомобилей
│   │   └── CarRepository.php              # Реализация репозитория автомобилей
│   └── User/
│       ├── UserRepositoryInterface.php    # Интерфейс репозитория пользователей
│       └── UserRepository.php             # Реализация репозитория пользователей
├── Services/
│   └── CarService.php                     # Бизнес-логика (использует DI)
└── Providers/
    └── AppServiceProvider.php             # Регистрация биндингов интерфейсов

database/
├── migrations/                            # Миграции БД
└── seeders/                               # Сидеры для тестовых данных

tests/
└── Feature/
    └── CarAvailableTest.php               # Feature тесты
```

---

## 🔒 Безопасность

- ✅ Аутентификация через Laravel Sanctum
- ✅ Валидация всех входящих данных через Form Requests
- ✅ Защита от SQL-инъекций через Eloquent ORM
- ✅ Типизация параметров методов
- ✅ Проверка прав доступа на уровне бизнес-логики

---

## 📝 Лицензия

MIT License

---

## 👨‍💻 Автор

Разработано с использованием принципов Clean Architecture и SOLID.

**Ключевые особенности реализации:**
- Четкое разделение слоев
- Следование принципам SOLID
- Тестируемый код
- Документированный API
- Профессиональная структура проекта
